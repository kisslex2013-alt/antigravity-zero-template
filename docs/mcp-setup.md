# 🔌 Настройка MCP серверов для Antigravity

Model Context Protocol (MCP) позволяет расширить возможности AI дополнительными инструментами. Для zero-coder это означает, что AI станет умнее в поиске кода и проверке версий библиотек.

## 🎯 Что такое MCP простыми словами?

MCP - это способ дать AI новые "суперспособности":
- **Умный поиск по коду** - AI понимает смысл, а не только ключевые слова
- **Проверка версий** - AI получает актуальные версии библиотек из реестров
- **Работа с разными проектами** - переключение между workspace

## 📦 Рекомендуемый MCP: Smart Coding MCP

### Что он умеет?

1. **🔍 Семантический поиск кода** (`a_semantic_search`)
   - Ищет код по смыслу, а не только по словам
   - Пример: "Где аутентификация?" найдёт код с "login", "session", "auth"
   - Работает даже с опечатками

2. **📦 Проверка версий пакетов** (`d_check_last_version`)
   - Поддержка npm, PyPI, Maven, Go, Rust и ещё 15+ экосистем
   - AI не будет угадывать версии - получит актуальные данные

3. **📂 Управление workspace** (`e_set_workspace`)
   - Переключение между проектами без перезапуска

4. **ℹ️ Статус и диагностика** (`f_get_status`)
   - Проверка здоровья сервера и индексации

## 🚀 Установка (пошагово)

### Шаг 1: Установите Smart Coding MCP

Откройте PowerShell и выполните:

```powershell
npm install -g smart-coding-mcp
```

**Что это делает:** Устанавливает MCP сервер глобально на ваш компьютер.

### Шаг 2: Настройте Antigravity

1. **Откройте файл конфигурации:**

```powershell
# Для Windows
notepad $env:USERPROFILE\.gemini\antigravity\mcp_config.json
```

2. **Добавьте конфигурацию:**

```json
{
  "mcpServers": {
    "smart-coding-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "smart-coding-mcp",
        "--workspace",
        "h:\\Backup\\Zero-Coding\\Antigravity\\new_project_template"
      ]
    }
  }
}
```

⚠️ **ВАЖНО:** Замените путь `h:\\Backup\\...` на **абсолютный путь к вашему проекту**!

💡 **Совет:** Antigravity НЕ поддерживает `${workspaceFolder}` - используйте только абсолютные пути.

### Шаг 3: Добавьте в .gitignore (чтобы не коммитить кеш)

Откройте `.gitignore` и добавьте:

```gitignore
# Smart Coding MCP cache
.smart-coding-cache/
```

### Шаг 4: Перезапустите Antigravity

Полностью закройте и откройте Antigravity IDE заново.

### Шаг 5: Проверьте работу

В чате с AI напишите:

```
Проверь статус MCP сервера
```

AI должен использовать инструмент `f_get_status` и показать информацию о сервере.

## 📋 Пример конфигурации для нескольких проектов

Если работаете с несколькими проектами, можете настроить разные MCP серверы:

```json
{
  "mcpServers": {
    "smart-coding-project-1": {
      "command": "npx",
      "args": ["-y", "smart-coding-mcp", "--workspace", "C:\\Projects\\project1"]
    },
    "smart-coding-project-2": {
      "command": "npx",
      "args": ["-y", "smart-coding-mcp", "--workspace", "C:\\Projects\\project2"]
    }
  }
}
```

## 🎓 Как AI будет использовать MCP

После настройки, AI автоматически начнёт использовать MCP инструменты благодаря правилам в `.antigravity/rules/smart-mcp.md`.

### Автоматическое поведение:

#### ✅ Для поиска кода:
```
Вы: Где обрабатывается логин пользователя?
AI: [Использует a_semantic_search вместо обычного grep]
AI: Нашёл в файле src/auth/login.ts функцию handleUserAuthentication()
```

#### ✅ Для проверки версий:
```
Вы: Добавь последнюю версию React
AI: [Использует d_check_last_version]
AI: Актуальная версия React: 18.3.1. Добавляю в package.json...
```

## 🔧 Расширенные настройки

### Изменение размерности эмбеддингов

Для более быстрой работы (но чуть менее точной):

```json
{
  "mcpServers": {
    "smart-coding-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "smart-coding-mcp",
        "--workspace",
        "путь/к/проекту",
        "--dimension",
        "256"
      ]
    }
  }
}
```

Доступные размерности: 64, 128, 256, 512, 768 (по умолчанию)

### Использование GPU (если есть CUDA)

```json
"args": [
  "-y",
  "smart-coding-mcp",
  "--workspace",
  "путь/к/проекту",
  "--device",
  "cuda"
]
```

## 🆘 Решение проблем

### Проблема: AI не использует MCP инструменты

**Решение:**
1. Проверьте правила в `.antigravity/rules/smart-mcp.md` - они должны быть на месте
2. Перезапустите Antigravity
3. Явно попросите: "Используй семантический поиск для..."

### Проблема: MCP сервер не запускается

**Решение:**
1. Проверьте установку: `npm list -g smart-coding-mcp`
2. Убедитесь, что путь в конфиге абсолютный
3. Проверьте логи Antigravity (Help → Toggle Developer Tools → Console)

### Проблема: Медленная индексация

**Решение:**
1. Первая индексация может занять время - это нормально
2. Используйте меньшую размерность эмбеддингов (256 вместо 768)
3. Добавьте большие файлы/папки в `.gitignore` если они не нужны для поиска

### Проблема: Кеш занимает много места

**Решение:**
Очистите кеш через AI:
```
Очисти кеш MCP сервера
```
AI использует инструмент `c_clear_cache`.

## 📚 Полезные команды для AI

После установки попробуйте:

```
Покажи статус индексации проекта
Найди где обрабатываются ошибки валидации
Какая последняя версия пакета typescript?
Переиндексируй проект
```

## 🔒 Приватность

- Все эмбеддинги генерируются **локальными моделями**
- Никакие данные проекта не отправляются в интернет
- Кеш хранится локально в `.smart-coding-cache/`

## 📖 Дополнительные ресурсы

- [Официальный репозиторий](https://github.com/omar-haris/smart-coding-mcp)
- [Документация по настройке](https://github.com/omar-haris/smart-coding-mcp/blob/main/docs/ide-setup/antigravity.md)

---

**Помните:** После установки MCP вам не нужно ничего делать - AI автоматически будет использовать умный поиск! 🎉

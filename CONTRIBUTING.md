# Contributing to Antigravity Zero Template

Спасибо за интерес к проекту! 🛸

## 🌟 Как помочь проекту

### Сообщить о баге
1. Проверьте, что баг ещё не описан в [Issues](../../issues)
2. Создайте новый Issue с подробным описанием

### Предложить улучшение
1. Откройте Issue с меткой `enhancement`
2. Опишите, какую проблему это решает

### Внести изменения в код
1. Форкните репозиторий
2. Создайте ветку: `git checkout -b feature/my-feature`
3. Внесите изменения
4. Запустите проверки: `.\scripts\verify.ps1`
5. Коммит: `git commit -m "feat: описание"`
6. Push: `git push origin feature/my-feature`
7. Откройте Pull Request

## 📝 Commit Convention

Используйте формат:
```
type(scope): description
```

**Типы:**
- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — документация
- `refactor` — рефакторинг
- `style` — форматирование
- `chore` — конфигурация

**Примеры:**
```bash
git commit -m "feat(workflows): add /deploy workflow"
git commit -m "fix(rules): correct security check"
git commit -m "docs: update README installation"
```

## 🔧 Локальная разработка

```powershell
# Клонирование
git clone https://github.com/YOUR_USERNAME/antigravity-zero-template.git
cd antigravity-zero-template

# Проверка
.\scripts\verify.ps1
```

## ✅ Чек-лист перед PR

- [ ] Код проходит `verify.ps1`
- [ ] Документация обновлена (если нужно)
- [ ] Коммиты следуют convention
- [ ] PR содержит описание изменений

## 💬 Вопросы?

Создайте Issue с меткой `question` или напишите в Discussions.

---

Спасибо за вклад! 🙏

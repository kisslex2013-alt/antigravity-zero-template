---
description: Детальная проработка спецификации через интервью
---

# Workflow: Deep-Dive Spec Interview (/spec)

## Назначение

Систематическая детализация технических спецификаций из грубых идей через процесс интервью с AI-агентом.

## Как использовать

### 1. Создайте файл спецификации

Создайте markdown-файл с вашей черновой идеей:

```powershell
# Создайте директорию для спецификаций (если её нет)
New-Item -Path ".\specs" -ItemType Directory -Force

# Создайте файл с идеей
New-Item -Path ".\specs\my-feature.md" -ItemType File -Value "# Новая фича`n`nКраткое описание идеи..."
```

### 2. Запустите интервью

Используйте команду:
```
/spec @specs/my-feature.md
```

### 3. Процесс интервью

AI-агент будет задавать вопросы по следующим темам:

#### Раунд 1: Основная механика
- Что именно делает фича?
- Какие основные действия доступны пользователю?
- Какие данные используются и как они обрабатываются?

#### Раунд 2: Пользовательский опыт
- Как выглядит интерфейс?
- Какие визуальные элементы нужны?
- Как пользователь взаимодействует с фичей?
- Какие анимации/переходы нужны?

#### Раунд 3: Техническая архитектура
- Какой стек технологий используется?
- Как организованы данные (структура, хранение)?
- Какие API/сервисы задействованы?
- Как обеспечивается безопасность?

#### Раунд 4: Граничные случаи
- Как обрабатываются ошибки?
- Что происходит в офлайн-режиме?
- Как решаются race conditions?
- Какие ограничения и валидации есть?

### 4. Финальный результат

AI-агент перезапишет исходный файл в детальную, готовую к реализации спецификацию.

## Пример структуры итоговой спецификации

```markdown
# Название фичи

## Обзор
Краткое описание фичи и её цели

## Основная механика
- Детальное описание функционала
- Пошаговые сценарии использования
- Логика работы

## UI/UX
- Макеты интерфейса (словесное описание или ссылки)
- Интерактивные элементы
- Анимации и переходы
- Адаптивность

## Техническая реализация
- Используемые технологии
- Структура данных
- API endpoints
- Схемы БД (если нужны)
- Безопасность

## Граничные случаи
- Обработка ошибок
- Офлайн функционал
- Конкурентные запросы
- Валидация данных

## Тестирование
- Критерии приемки
- Тест-кейсы
- Edge cases для тестирования
```

## Почему это полезно?

✅ **Предотвращает неоднозначность**: Заставляет принять решения по "очевидным" вещам, которые на самом деле не очевидны

✅ **Экономит время разработки**: Кодить по строгой спецификации намного быстрее, чем гадать

✅ **Документирует решения**: Финальная спецификация служит источником истины для проекта

✅ **Улучшает качество**: Продуманные заранее edge cases и архитектура снижают количество багов

## Интеграция с другими артефактами

После завершения интервью:

1. **Используйте спецификацию** для создания `artifacts/implementation_plan.md`
2. **Разбейте на задачи** в `artifacts/task.md`
3. **Следуйте плану** при реализации
4. **Документируйте результат** в `artifacts/walkthrough.md`

## Советы по использованию

- 📝 Начинайте с простого описания идеи - агент поможет детализировать
- 🎯 Отвечайте конкретно на вопросы агента
- 🔄 Не бойтесь возвращаться к предыдущим раундам если что-то изменилось
- 💡 Используйте примеры из существующего кода для ясности
- ⚡ После первого раунда можете попросить агента сгенерировать визуальные примеры

## Пример использования

```markdown
# Исходный файл specs/notification-system.md

Хочу систему уведомлений с красивыми анимациями

---

# После интервью тот же файл превратится в:

# Система уведомлений

## Обзор
Модульная система toast-уведомлений с поддержкой множественных вариантов отображения...

[Детальная спецификация на 50+ строк]
```

---

**Помните**: Качественная спецификация - залог быстрой и правильной реализации!

---
description: Комплексная проверка кода тестами
---

# Workflow: Verify (Проверка) 🔬

## Для кого
Для **zero-coder**, кто хочет убедиться, что код работает правильно во всех ситуациях.

## Что это
Создаёт и запускает **автоматические тесты** для вашего кода.
Тесты проверяют, что код работает правильно даже в неожиданных случаях.

## Когда использовать

✅ **Используйте:**
- После `/simplify` - проверить что ничего не сломалось
- Перед релизом - убедиться что всё работает
- После исправления бага - убедиться что не появится снова
- Когда есть спецификация с критериями

❌ **Не используйте:**
- На коде, который ещё не работает
- Если тесты уже есть и проходят

## Как использовать

### Вариант 1: Проверить конкретный файл
```
/verify @src/components/LoginForm.jsx
```

### Вариант 2: Проверить по спецификации
```
/verify @specs/login-form.md
```

### Вариант 3: Проверить что исправлен баг
```
/verify Проверить что email валидация работает
```

## Инструкция для AI

При запуске `/verify`:

### Шаг 1: Понять что проверять (1-2 минуты)

1. **Если указан файл кода** → анализируй что он делает
2. **Если указана спецификация** → возьми критерии оттуда
3. **Если описание** → попроси прояснить детали

Определи:
- Какие функции нужно протестировать
- Какие сценарии (happy path)
- Какие edge cases (граничные случаи)
- Какие ошибки могут быть

### Шаг 2: Создать план тестирования

Создай `artifacts/verify-plan.md`:

```markdown
# 🔬 План тестирования: [Название]

## Что тестируем
Файл: `src/components/LoginForm.jsx`
Функции: handleLogin, validateEmail, validatePassword

## Сценарии для проверки

### ✅ Happy Path (всё хорошо)
- [ ] Пользователь вводит правильный email и пароль
- [ ] Нажимает кнопку "Войти"
- [ ] Успешно входит в систему

### ⚠️ Граничные случаи (Edge Cases)
- [ ] Email без @ (должна быть ошибка)
- [ ] Пустой пароль (должна быть ошибка)
- [ ] Слишком короткий пароль (должна быть ошибка)
- [ ] Слишком длинные значения (limit test)

### ❌ Ошибки (Error Cases)
- [ ] Сервер не отвечает (timeout)
- [ ] Неверные учетные данные (401)
- [ ] Нет интернета (offline)

### 🔄 Интеграции
- [ ] API вызов отправляется правильно
- [ ] Loading state показывается
- [ ] Редирект после успеха

## Будут созданы тесты
1. `LoginForm.test.jsx` - тесты компонента
2. `validators.test.js` - тесты валидации
3. `auth.test.js` - тесты API

Продолжить? (да/нет)
```

### Шаг 3: Дождаться подтверждения
Покажи план через `notify_user` и дождись "да".

### Шаг 4: Создать тесты (после подтверждения)

#### Правила написания тестов для zero-coder:

1. **Понятные названия**
```javascript
// ✅ Хорошо (понятно что проверяем):
test('показывает ошибку если email без @', () => {
  ...
});

// ❌ Плохо (непонятно):
test('test1', () => {
  ...
});
```

2. **Структура AAA (Arrange-Act-Assert)**
```javascript
test('валидация email работает правильно', () => {
  // Arrange (Подготовка)
  const email = 'test@example.com';
  
  // Act (Действие)
  const result = validateEmail(email);
  
  // Assert (Проверка)
  expect(result).toBe(true);
});
```

3. **Один тест = одна проверка**
```javascript
// ✅ Хорошо:
test('принимает правильный email', () => {
  expect(validateEmail('test@example.com')).toBe(true);
});

test('отклоняет email без @', () => {
  expect(validateEmail('test.com')).toBe(false);
});

// ❌ Плохо (слишком много в одном):
test('валидация email', () => {
  expect(validateEmail('test@example.com')).toBe(true);
  expect(validateEmail('test.com')).toBe(false);
  expect(validateEmail('')).toBe(false);
  // ... ещё 10 проверок
});
```

4. **Комментарии для сложных тестов**
```javascript
test('отправляет правильные данные в API', async () => {
  // Настраиваем mock API
  const mockFetch = jest.fn();
  global.fetch = mockFetch;
  
  // Вызываем функцию логина
  await login('user@test.com', 'password123');
  
  // Проверяем что API вызван с правильными параметрами
  expect(mockFetch).toHaveBeenCalledWith('/api/login', {
    method: 'POST',
    body: JSON.stringify({
      email: 'user@test.com',
      password: 'password123'
    })
  });
});
```

5. **Проверка Edge Cases**
```javascript
// Пустые значения
test('отклоняет пустой email', () => {
  expect(validateEmail('')).toBe(false);
});

// Пробелы
test('отклоняет email с пробелами', () => {
  expect(validateEmail('test @example.com')).toBe(false);
});

// Экстремальные значения
test('принимает очень длинный email', () => {
  const longEmail = 'a'.repeat(50) + '@example.com';
  expect(validateEmail(longEmail)).toBe(true);
});

// Null/undefined
test('отклоняет null', () => {
  expect(validateEmail(null)).toBe(false);
});
```

### Шаг 5: Запустить тесты

1. Создай скрипт для запуска (если нет):
```json
// package.json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  }
}
```

2. Запусти тесты:
```bash
npm test
```

3. Покажи результаты pользователю

### Шаг 6: Документирование результатов

Создай `artifacts/verify-report.md`:

```markdown
# 🔬 Отчёт о тестировании: [Название]

## Результаты

### ✅ Всё прошло успешно
```
PASS  src/components/LoginForm.test.jsx
  ✓ показывает форму логина (25ms)
  ✓ валидирует email (12ms)
  ✓ валидирует пароль (10ms)
  ✓ отправляет данные при submit (45ms)
  ✓ показывает ошибку при неверных данных (30ms)

Tests: 5 passed, 5 total
Time: 2.5s
```

### 📊 Покрытие тестами
- `LoginForm.jsx`: 100% (все строки)
- `validators.js`: 80% (пропущены edge cases)
- `auth.js`: 90% (пропущена обработка timeout)

### ⚠️ Что НЕ покрыто тестами
1. **timeout при медленном интернете**
   - Где: `auth.js` строка 45
   - Риск: Пользователь может ждать вечно
   - TODO: Добавить тест с setTimeout

2. **очень длинный пароль (>1000 символов)**
   - Где: `validators.js`
   - Риск: Низкий, но стоит проверить
   - TODO: Добавить edge case тест

## Найденные баги 🐛

### Баг #1: Форма отправляется даже если данные невалидны
**Где:** `LoginForm.jsx` строка 67
**Как воспроизвести:**
1. Введите невалидный email
2. Нажмите Enter
3. Форма отправляется (не должна!)

**Как исправить:**
```javascript
// Добавить проверку перед submit
if (!isValid) {
  e.preventDefault();
  return;
}
```

**Хотите, чтобы я исправил? (да/нет)**

## Рекомендации
1. ✅ Основной функционал протестирован
2. ⚠️ Добавьте тесты для timeout
3. ⚠️ Исправьте Баг #1
4. 💡 Рассмотрите добавление E2E тестов

## Следующие шаги
1. Проверьте найденные баги
2. Если всё ОК → можно выкатывать
3. Если есть проблемы → я исправлю
```

### Шаг 7: Показать результаты
Используй `notify_user` с отчётом.

## Примеры использования

### Пример 1: Проверка после Simplify
```
👤 Пользователь: /verify @src/components/RegisterForm.jsx

🤖 AI:
Анализирую RegisterForm.jsx...

План тестирования:
✅ 3 happy path сценария
⚠️ 5 edge cases
❌ 3 error cases

Создам файл `RegisterForm.test.jsx` с 11 тестами.
Продолжить? (да/нет)

👤 Пользователь: да

🤖 AI:
Создаю тесты... ✅
Запускаю тесты...

Результат:
✅ 10 passed
❌ 1 failed

Найден баг! Форма принимает пароль из 1 символа, 
а в спецификации минимум 8.

Хотите, чтобы я исправил? (да/нет)

👤 Пользователь: да

🤖 AI: ✅ Исправлено!
Перезапускаю тесты... ✅ Все 11 тестов прошли!

Отчёт сохранён в artifacts/verify-report.md
```

### Пример 2: Проверка по спецификации
```
👤 Пользователь: /verify @specs/login-form.md

🤖 AI:
Читаю спецификацию...

Критерии приёмки из specs/login-form.md:
- [x] Email валидация
- [x] Пароль минимум 8 символов
- [x] Loading state при отправке
- [ ] Remember me checkbox ← НЕ РЕАЛИЗОВАНО!

Проблема: В коде нет "Remember me", хотя в спеке есть.

Варианты:
1. Добавить "Remember me" в код
2. Удалить из спецификации (не нужно)

Что делаем? (1/2)
```

### Пример 3: Регрессионное тестирование
```
👤 Пользователь: /verify Убедись что баг #42 исправлен

🤖 AI:
Ищу информацию о баге #42...
Нужны детали. Опишите баг:
- Что было не так?
- Как воспроизвести?
- Что исправили?

👤 Пользователь: Email с + валидировался как неверный

🤖 AI:
Понял! Создаю регрессионный тест:

test('принимает email с символом +', () => {
  expect(validateEmail('user+tag@example.com')).toBe(true);
});

Запускаю... ✅ Тест прошёл!
Баг #42 исправлен и больше не появится.
```

## Типичные ошибки

### ❌ Ошибка 1: Тесты "зелёные", но код не работает
**Причина:** Тесты проверяют не то
**Решение:** Скажите "Добавь реальный сценарий использования"

### ❌ Ошибка 2: Слишком много тестов
**Если AI создал 100+ тестов:**
→ Скажите "Оставь только самые важные: happy path + главные edge cases"

### ❌ Ошибка 3: Тесты не запускаются
**Если ошибки при npm test:**
→ Скажите "Установи недостающие зависимости"

## Связь с другими workflows

```
/draft → /simplify → /verify
   ↓        ↓          ↓
Быстро  Красиво    Надёжно ← ВЫ ЗДЕСЬ
```

**После `/verify`:**
- ✅ Если всё прошло → можно релизить
- ⚠️ Если найдены баги → исправить и `/verify` снова
- 📝 Обновить changelog (я могу помочь)

---

## Философия Verify

> "Работает на моём компьютере ≠ Работает везде"

Цель Verify - **гарантировать качество**.

**Что проверяют тесты:**
- ✅ Код делает то, что должен (функциональность)
- ⚠️ Код НЕ делает то, что не должен (безопасность)
- 🔄 Код работает в разных ситуациях (робастность)
- 💥 Код корректно обрабатывает ошибки (надёжность)

**Помните:**
- Тесты = страховка от багов
- Тесты = документация как код работает
- Тесты = уверенность при изменениях

**Zero-coder friendly:**
Не бойтесь тестов! Я создам их понятными с комментариями.
Вам нужно только проверить результаты.

---

**Команда запуска:** `/verify @путь/к/файлу` или `/verify @specs/file.md`

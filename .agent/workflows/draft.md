---
description: Быстрое создание рабочего прототипа
---

# Workflow: Draft (Прототип) 🚀

## Для кого
Для **zero-coder**, кто хочет быстро получить "работающий код", даже если он не идеальный.

## Что это
Создаёт **минимальный рабочий прототип** (MVP) вашей идеи за минимальное время. 
Код будет работать, но может быть "некрасивым" - это нормально! Потом улучшим через `/simplify`.

## Когда использовать

✅ **Используйте:**
- Когда нужно быстро проверить идею
- Когда хотите увидеть результат сейчас
- Когда есть спецификация (из `/spec`)
- Для первой версии фичи

❌ **Не используйте:**
- Если нужен идеальный код (используйте сразу `/pipeline`)
- Если нет спецификации (сначала `/spec`)

## Как использовать

### Вариант 1: Есть файл спецификации

```
/draft @specs/my-feature.md
```

**Что произойдёт:**
1. AI прочитает вашу спецификацию
2. Создаст рабочий код максимально быстро
3. Добавит TODO-комментарии где можно улучшить
4. Покажет вам результат

### Вариант 2: Быстрое описание

```
/draft Хочу форму регистрации с email и паролем
```

**Что произойдёт:**
1. AI задаст 1-2 быстрых вопроса
2. Сразу создаст прототип
3. Предложит дальнейшие улучшения

## Инструкция для AI

При запуске `/draft`:

### Шаг 1: Понять задачу (1 минута)
1. Прочитать спецификацию (если есть файл)
2. Или попросить описание задачи (если нет файла)
3. Определить минимальные требования для работающего прототипа

### Шаг 2: Быстрый прототип (Главная фаза)
**Режим работы: SPEED > QUALITY**

Правила создания прототипа:
- ✅ Код должен **работать**
- ✅ Используй **простейшие решения**
- ✅ Не пиши тесты (потом в `/verify`)
- ✅ Не оптимизируй производительность
- ✅ Копируй существующие паттерны из проекта
- ✅ Добавляй `// TODO:` комментарии для улучшений

**Что включить в прототип:**
```javascript
// ✅ Основная функциональность
function createUser(email, password) {
  // TODO: Добавить валидацию email
  // TODO: Добавить хеширование пароля
  return { email, password };
}

// ✅ Базовый UI (если нужен)
<form>
  <input type="email" />
  <input type="password" />
  <button>Register</button>
  {/* TODO: Добавить стили */}
  {/* TODO: Добавить валидацию на клиенте */}
</form>

// ❌ НЕ делать в прототипе:
// - Сложные проверки
// - Красивые стили
// - Обработку всех edge cases
// - Тесты
```

### Шаг 3: Документирование (Важно!)
После создания кода, создай в `artifacts/` файл `draft-report.md`:

```markdown
# 🚀 Прототип: [Название]

## Что сделано
- ✅ [Основная функция 1]
- ✅ [Основная функция 2]

## Файлы
- `src/feature.js` - Основная логика
- `src/components/Form.jsx` - UI компонент

## Как проверить
1. Запустите проект: `npm run dev`
2. Откройте: http://localhost:3000/feature
3. Попробуйте: [действия для проверки]

## TODO (что улучшить)
- [ ] Добавить валидацию (используйте `/simplify`)
- [ ] Добавить стили (используйте `/simplify`)
- [ ] Добавить тесты (используйте `/verify`)

## Следующие шаги
1. **Проверьте прототип** - работает ли основная функция?
2. **Если да** → `/simplify @src/feature.js` для улучшения
3. **Если нет** → опишите проблему, я исправлю
```

### Шаг 4: Показать пользователю
Используй `notify_user` чтобы показать:
- Что создано
- Как проверить
- Что делать дальше

## Примеры использования

### Пример 1: Форма логина
```
👤 Пользователь: /draft @specs/login-form.md

🤖 AI: 
Создаю прототип формы логина...

Создано:
- src/components/LoginForm.jsx
- src/api/auth.js

Прототип готов! Проверьте:
npm run dev → http://localhost:3000/login

TODO для улучшения:
- Валидация email
- Красивые стили
- Обработка ошибок

Следующий шаг: /simplify @src/components/LoginForm.jsx
```

### Пример 2: API endpoint
```
👤 Пользователь: /draft Нужен API для получения списка пользователей

🤖 AI: 
Быстрый вопрос: откуда брать данные?
1. Из базы данных
2. Из файла (mock data)
3. Другое

👤 Пользователь: 2

🤖 AI:
Создан прототип API endpoint:
- GET /api/users возвращает список из mockData.json

Файлы:
- src/api/users.js
- src/data/mockData.json

Проверка: curl http://localhost:3000/api/users

TODO: подключить реальную БД (используйте /simplify)
```

## Типичные ошибки

### ❌ Ошибка 1: AI слишком долго думает
**Решение:** Напишите "Сделай максимально просто, главное чтобы работало"

### ❌ Ошибка 2: Код не запускается
**Решение:** Опишите ошибку AI, он быстро исправит

### ❌ Ошибка 3: AI предлагает много улучшений
**Решение:** Скажите "Оставь TODO на потом, покажи что есть"

## Связь с другими workflows

```
/spec     → /draft → /simplify → /verify
   ↓          ↓         ↓           ↓
Детально  Быстро   Красиво    Надёжно
описать   сделать  сделать    проверить
```

**Рекомендуемый порядок:**
1. `/spec` - детально описать что нужно  
2. **`/draft`** - создать работающий прототип ← ВЫ ЗДЕСЬ
3. `/simplify` - улучшить код
4. `/verify` - проверить тестами

**Или используйте `/pipeline` для автоматического прохождения всех шагов!**

---

## Философия Draft

> "Работающий плохой код лучше, чем идеальный код в голове"

Цель Draft - **убрать страх начать**. Не бойтесь "плохого" кода в прототипе - это временно!

**Помните:** 
- Draft ≠ финальная версия
- Draft = первый шаг к результату
- Улучшения делаем потом через `/simplify`

---

**Команда запуска:** `/draft` или `/draft @specs/file.md`

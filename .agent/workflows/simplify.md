---
description: Улучшение и упрощение существующего кода
---

# Workflow: Simplify (Упрощение) 🧹

## Для кого
Для **zero-coder**, кто хочет сделать код чище, понятнее и легче поддерживать.

## Что это
Улучшает существующий код: убирает дублирование, делает читаемее, добавляет комментарии. 
Превращает "работает, но сложно" в "работает и понятно".

## Когда использовать

✅ **Используйте:**
- После `/draft` - улучшить прототип
- Когда код работает, но сложно читается
- Когда много повторяющегося кода
- Когда нужно добавить комментарии

❌ **Не используйте:**
- На коде, который не работает (сначала исправьте)
- Если код уже простой и понятный

## Как использовать

### Вариант 1: Упростить конкретный файл
```
/simplify @src/components/LoginForm.jsx
```

### Вариант 2: Упростить несколько файлов
```
/simplify @src/auth/login.js @src/auth/register.js
```

### Вариант 3: Описать что улучшить
```
/simplify Слишком длинная функция handleSubmit в LoginForm
```

## Инструкция для AI

При запуске `/simplify`:

### Шаг 1: Анализ кода (2-3 минуты)
1. Прочитать указанный файл(ы)
2. Найти проблемы:
   - Дублирование кода
   - Слишком длинные функции (>30 строк)
   - Непонятные названия переменных
   - Отсутствие комментариев
   - Сложная вложенность (if в if в if...)
   - Magic numbers (непонятные числа в коде)

### Шаг 2: Показать план улучшений
**Важно:** Сначала покажи план, НЕ меняй код сразу!

Создай в `artifacts/simplify-plan.md`:

```markdown
# 🧹 План упрощения: [Название файла]

## Найденные проблемы

### 1. Дублирование кода
**Где:** строки 45-60 и 80-95
**Что:** Одинаковая логика валидации email
**Как исправить:** Вынести в функцию `validateEmail()`

### 2. Длинная функция
**Где:** `handleSubmit` (78 строк)
**Что:** Делает слишком много: валидация + API + UI
**Как исправить:** Разбить на 3 функции

### 3. Magic number
**Где:** `if (password.length < 8)`
**Что:** Непонятно откуда число 8
**Как исправить:** `const MIN_PASSWORD_LENGTH = 8`

## Предложенные изменения

### Было:
```javascript
function handleSubmit() {
  if (email.includes('@')) {
    if (password.length > 8) {
      fetch('/api/login', ...)
    }
  }
}
```

### Станет:
```javascript
const MIN_PASSWORD_LENGTH = 8;

function validateEmail(email) {
  return email.includes('@');
}

function validatePassword(password) {
  return password.length >= MIN_PASSWORD_LENGTH;
}

async function handleSubmit() {
  if (!validateEmail(email)) return;
  if (!validatePassword(password)) return;
  await loginUser(email, password);
}
```

## Что улучшится?
- ✅ Легче читать
- ✅ Легче тестировать
- ✅ Легче изменять

Продолжить? (да/нет)
```

### Шаг 3: Дождаться подтверждения
Используй `notify_user` чтобы показать план и спросить разрешение.

**Не меняй код без подтверждения!**

### Шаг 4: Применить изменения (после "да")

Правила упрощения:

#### 1. Убрать дублирование
```javascript
// ❌ Было (дублирование):
function loginUser() {
  if (!email.includes('@')) {
    alert('Invalid email');
    return;
  }
  // ...
}

function registerUser() {
  if (!email.includes('@')) {
    alert('Invalid email');
    return;
  }
  // ...
}

// ✅ Стало (одна функция):
function validateEmail(email) {
  if (!email.includes('@')) {
    alert('Invalid email');
    return false;
  }
  return true;
}
```

#### 2. Разбить длинные функции
```javascript
// ❌ Было (78 строк в одной функции):
function handleSubmit() {
  // валидация
  // API запрос
  // обновление UI
  // логирование
  // редирект
}

// ✅ Стало (5 маленьких функций):
function validateForm() { ... }
function submitToAPI() { ... }
function updateUI() { ... }
function logSuccess() { ... }
function redirectUser() { ... }

async function handleSubmit() {
  if (!validateForm()) return;
  await submitToAPI();
  updateUI();
  logSuccess();
  redirectUser();
}
```

#### 3. Понятные названия
```javascript
// ❌ Плохо:
const x = 5;
function f(a, b) { return a + b; }

// ✅ Хорошо:
const maxRetries = 5;
function calculateTotal(price, tax) { return price + tax; }
```

#### 4. Добавить комментарии (для сложных мест)
```javascript
// ✅ Хороший комментарий (объясняет ПОЧЕМУ):
// Используем setTimeout, чтобы дать браузеру время отрисовать UI
setTimeout(heavyCalculation, 0);

// ❌ Плохой комментарий (повторяет код):
// Увеличиваем counter на 1
counter++;
```

#### 5. Константы вместо magic numbers
```javascript
// ❌ Было:
if (age < 18) { ... }
setTimeout(check, 5000);

// ✅ Стало:
const ADULT_AGE = 18;
const CHECK_INTERVAL_MS = 5000;

if (age < ADULT_AGE) { ... }
setTimeout(check, CHECK_INTERVAL_MS);
```

### Шаг 5: Документирование
После изменений создай `artifacts/simplify-report.md`:

```markdown
# 🧹 Отчёт об упрощении: [Файл]

## Что изменено

### Изменение 1: Убрано дублирование
- Было: 45 строк повторяющегося кода
- Стало: 1 переиспользуемая функция
- Экономия: 44 строки

### Изменение 2: Разбита длинная функция
- Было: `handleSubmit` 78 строк
- Стало: 5 функций по 10-15 строк
- Плюс: теперь легко тестировать каждую отдельно

### Изменение 3: Добавлены константы
- Было: 3 magic numbers
- Стало: 3 именованные константы

## Метрики

| Показатель | Было | Стало | Улучшение |
|------------|------|-------|-----------|
| Строк кода | 234  | 198   | -36 строк |
| Функций    | 3    | 8     | Легче читать |
| Дублирование | 3 места | 0 | ✅ |

## Код всё ещё работает?
✅ Да! Я сохранил функциональность, изменил только структуру.

## Следующие шаги
1. Проверьте что всё работает: `npm run dev`
2. Если ОК → `/verify @src/components/LoginForm.jsx` для тестов
3. Если проблемы → опишите, я исправлю
```

### Шаг 6: Показать результат
Используй `notify_user` с результатами и предложи следующий шаг.

## Примеры использования

### Пример 1: Упрощение после Draft
```
👤 Пользователь: /simplify @src/components/RegisterForm.jsx

🤖 AI:
Анализирую RegisterForm.jsx...

Найдено 3 проблемы:
1. Дублирование: валидация email повторяется 2 раза
2. Длинная функция: handleSubmit (65 строк)
3. Magic numbers: 8, 20, 100

План:
- Создать validateEmail()
- Разбить handleSubmit на 4 функции
- Добавить константы MIN_LENGTH, MAX_LENGTH

Показать подробный план? (да/нет)

👤 Пользователь: да

🤖 AI: [показывает detailed план из artifacts/simplify-plan.md]

Применить изменения? (да/нет)

👤 Пользователь: да

🤖 AI: ✅ Готово!
- Убрано 23 строки дублирования
- Создано 4 маленькие функции вместо 1 большой
- Добавлены константы

Код стал на 15% короче и в 3 раза читабельнее!

Следующий шаг: /verify для тестирования
```

### Пример 2: Целенаправленное улучшение
```
👤 Пользователь: /simplify Функция calculatePrice слишком сложная

🤖 AI:
Нашёл calculatePrice в src/utils/pricing.js (45 строк)

Проблемы:
- 4 уровня вложенности if
- 6 magic numbers
- Непонятная логика скидок

Предлагаю:
1. Разбить на calculateDiscount() и applyTax()
2. Заменить if-цепочки на объект конфигурации
3. Добавить комментарии

Показать код "до/после"? (да/нет)
```

## Типичные ошибки

### ❌ Ошибка 1: AI меняет логику
**Если AI изменил, КАК работает код:**
→ Скажите "Верни функциональность как было, меняй только структуру"

### ❌ Ошибка 2: Слишком много изменений
**Если AI переписал весь файл:**
→ Скажите "Меняй по одной проблеме за раз"

### ❌ Ошибка 3: Код перестал работать
**Если что-то сломалось:**
→ Скажите "Откати изменения, что-то сломалось: [опишите]"

## Связь с другими workflows

```
/draft → /simplify → /verify
   ↓        ↓          ↓
Быстро  Красиво    Надёжно
сделать сделать    проверить
```

**Рекомендуемый порядок:**
1. `/draft` - создать работающий код  
2. **`/simplify`** - улучшить код ← ВЫ ЗДЕСЬ
3. `/verify` - проверить тестами

---

## Философия Simplify

> "Код пишется один раз, а читается сто раз"

Цель Simplify - сделать код **понятным для будущего себя**.

**Правила хорошего кода:**
- Если функция >30 строк → разбей
- Если код повторяется 2+ раза → вынеси в функцию
- Если переменная называется `x` → переименуй понятно
- Если есть число в коде → сделай константой

**Помните:**
- Простой код = меньше багов
- Простой код = легче изменять
- Простой код = быстрее разбираться

---

**Команда запуска:** `/simplify @путь/к/файлу.js`

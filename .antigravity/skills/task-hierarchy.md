# 📋 Task Hierarchy & Tracking

Система иерархического трекинга задач, вдохновлённая [Beads](https://github.com/steveyegge/beads).

## Формат задач

### Простые задачи
```markdown
- [ ] Простая задача
- [x] Выполненная задача
- [/] В процессе
```

### Иерархические задачи (Epic → Task → Sub-task)
```markdown
## E1: Epic Name (большая фича)
- [ ] T1: Основная задача
  - [ ] T1.1: Подзадача 1
  - [ ] T1.2: Подзадача 2
- [ ] T2: Другая задача
  - [ ] T2.1: Детальный шаг
```

## Commit Convention

При коммите, связанном с задачей, добавляй ID в конце сообщения:

```bash
git commit -m "feat(auth): add login form (T1.2)"
git commit -m "fix(chart): correct tooltip position (T3)"
git commit -m "docs: update README (E1)"
```

**Формат ID:**
- `E[N]` — Epic (большая фича)
- `T[N]` — Task (задача)
- `T[N].[M]` — Sub-task (подзадача)

## Статусы

| Маркер | Статус | Описание |
|--------|--------|----------|
| `[ ]` | Pending | Не начато |
| `[/]` | In Progress | В работе |
| `[x]` | Done | Завершено |
| `[~]` | Blocked | Заблокировано |
| `[-]` | Cancelled | Отменено |

## Пример task.md

```markdown
# Текущие задачи

## E1: Авторизация пользователей
- [x] T1: Дизайн формы логина
  - [x] T1.1: Макет в Figma
  - [x] T1.2: Адаптивная вёрстка
- [/] T2: Интеграция с Supabase
  - [x] T2.1: Настройка проекта
  - [ ] T2.2: Компонент AuthModal
  - [ ] T2.3: Хранение токенов
- [ ] T3: Восстановление пароля

## E2: Дашборд аналитики
- [ ] T1: Компоненты графиков
- [ ] T2: API для данных
```

## Интеграция с Git (опционально)

### Установка Beads CLI
Для продвинутого трекинга можно установить Beads:

```bash
# npm
npm install -g @beads/bd

# После установки
bd init
bd create "Название задачи" -p 1
bd ready  # Показать готовые к работе задачи
```

**Преимущества Beads:**
- ✅ Git-native: задачи хранятся как JSONL в `.beads/`
- ✅ Hash-based ID: `bd-a1b2` — нет конфликтов при merge
- ✅ Dependency tracking: задачи с зависимостями
- ✅ Auto-sync: синхронизация с Git

**Когда использовать:**
- Проект с командой
- Много параллельных веток
- Нужна история задач в Git

> 💡 **Для zero-coder**: Простой формат `task.md` достаточен для большинства случаев. Beads — для продвинутых сценариев.

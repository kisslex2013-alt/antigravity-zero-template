# AGENTS.md

Это файл с инструкциями для AI-агентов при работе с проектом, созданным из этого шаблона.

## 🎯 О проекте

Этот проект создан на основе универсального шаблона для быстрого старта разработки.

## 📂 Структура проекта

```
project/
├── .agent/              # Конфигурация и workflows для AI-агентов
│   ├── workflows/       # Автоматизированные workflow
│   └── architect.md     # Архитектурные решения
├── .vscode/             # Настройки VS Code
├── artifacts/           # Артефакты текущей сессии (планы, задачи)
├── changelog/           # История изменений проекта
├── docs/                # Документация
├── scripts/             # Скрипты для автоматизации
└── src/                 # Исходный код приложения
```

## 🚀 Команды для работы

### Настройка окружения
```powershell
# Инициализация проекта (первый запуск)
.\scripts\setup.ps1

# Проверка готовности окружения
.\scripts\verify.ps1
```

### Работа с артефактами
- **Планы** создаются в `artifacts/implementation_plan.md`
- **Задачи** отслеживаются в `artifacts/task.md`
- **Walkthrough** документируются в `artifacts/walkthrough.md`

### Работа с документацией
- Изучите `ARCHITECTURE.md` для понимания структуры
- Читайте `changelog/changelog.md` для истории изменений
- Используйте `.agent/architect.md` для архитектурных решений

## 📝 Правила работы с проектом

### При создании новых фич
1. **Планирование**: Создайте детальный план в `artifacts/implementation_plan.md`
2. **Задачи**: Разбейте задачу на чек-лист в `artifacts/task.md`
3. **Реализация**: Следуйте плану и отмечайте выполненные пункты
4. **Документация**: После завершения создайте `artifacts/walkthrough.md`

### Структура кода
- Используйте существующую архитектуру из `ARCHITECTURE.md`
- Следуйте принципам, описанным в `.agent/architect.md`
- Обновляйте `changelog.md` при значимых изменениях

### Тестирование
- Проверяйте корректность с помощью `scripts\verify.ps1`
- Тестируйте все критические изменения
- Документируйте результаты тестирования

## 🔄 Workflows

Проект поддерживает автоматизированные workflows в директории `.agent/workflows/`:

- **`/spec`** - Детальная проработка спецификаций через интервью
- **`/brainstorm`** - Режим мозгового штурма для поиска лучшего решения
- Другие workflows - см. содержимое `.agent/workflows/`

Для использования workflow: `/workflow-name @path/to/file.md`

## ⚠️ Важные примечания

### Для AI-агентов
- Всегда читайте `ARCHITECTURE.md` перед началом работы
- Используйте существующие паттерны из кодовой базы
- Обновляйте документацию при изменениях
- Следуйте структуре артефактов (task.md → implementation_plan.md → код → walkthrough.md)
- **Важно**: Следуйте правилам из `.antigravity/rules.md`:
  - ⚠️ `Pause When Uncertain` — остановись при неясностях
  - 📋 `Output Checklist` — структурируй ответы
  - 🔒 `Security Essentials` — никогда не выводи секреты

### Файлы конфигурации
- `.vscode/settings.json` - настройки редактора
- `.vscode/extensions.json` - рекомендуемые расширения
- `.agent/` - конфигурация для AI-агентов

## 📖 Дополнительная информация

При возникновении вопросов:
1. Изучите `README.md` проекта
2. Проверьте `docs/` директорию
3. Просмотрите существующие примеры в коде
4. Используйте workflow `/spec` для детальной проработки

---

**Помните**: Этот файл помогает AI-агентам лучше понимать структуру проекта и правила работы с ним.

---

## 🎭 AI Personas & Skills

Этот проект поддерживает расширенные возможности AI:

1. **Personas**: Вы можете принимать разные роли (Architect, Debugger, Reviewer). См. `.antigravity/personas.md`.
2. **Brainstorm**: Для сложных задач используйте workflow `/brainstorm`, чтобы сгенерировать 2-3 альтернативных подхода.
3. **Skills**: Проверяйте `.antigravity/skills/` для активации специализированных навыков:
   - `context-engineering.md` — оптимизация работы с контекстом
   - `task-hierarchy.md` — иерархия задач и commit convention

## 📝 Commit Convention

При коммите используй формат с ID задачи из `task.md`:

```bash
# Формат
git commit -m "type(scope): description (T1.2)"

# Примеры
git commit -m "feat(auth): add login form (T1.2)"
git commit -m "fix(chart): correct tooltip position (T3)"
git commit -m "docs: update README (E1)"
```

**Типы коммитов:**
- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — документация
- `refactor` — рефакторинг
- `style` — форматирование
- `perf` — оптимизация
- `chore` — конфигурация/сборка

> 💡 ID задачи (`T1`, `T1.2`, `E1`) связывает коммит с задачей в `task.md`

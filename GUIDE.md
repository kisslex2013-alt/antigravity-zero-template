# 📖 Руководство по работе с шаблоном проекта

Это руководство для **zero-coder** - объясняет простым языком, как работать с этим шаблоном и AI-агентами.

## 🎯 Что это за шаблон?

Это готовая структура для быстрого старта проекта с AI. Вместо того, чтобы создавать файлы и папки с нуля, вы получаете готовый каркас с настроенными инструментами для работы с AI-агентами (Gemini, Claude и др.).

## 🚀 Первый запуск

### 1. Инициализация проекта

```powershell
# Запустите скрипт настройки (один раз при создании проекта)
.\scripts\setup.ps1
```

Этот скрипт:
- Создаст нужные директории
- Настроит структуру проекта
- Подготовит файлы для работы

### 2. Проверка готовности

```powershell
# Проверьте, что всё настроено правильно
.\scripts\verify.ps1
```

## 📁 Структура проекта (что где лежит)

```
project/
├── .agent/              # 🤖 Инструкции для AI-агентов
│   ├── workflows/       # Автоматические сценарии работы
│   └── architect.md     # Роль AI-архитектора
│
├── artifacts/           # 📄 Рабочие документы текущей сессии
│   ├── task.md          # Чек-лист текущих задач
│   ├── implementation_plan.md  # План реализации
│   └── walkthrough.md   # Отчёт о проделанной работе
│
├── changelog/           # 📝 История всех изменений
│   └── changelog.md     # Что и когда менялось
│
├── scripts/             # 🔧 Полезные скрипты
│   ├── setup.ps1        # Первичная настройка
│   └── verify.ps1       # Проверка проекта
│
├── src/                 # 💻 Ваш код здесь!
│
├── AGENTS.md            # Инструкции для AI (не трогайте)
├── ARCHITECTURE.md      # Архитектура проекта
├── mission.md           # Цель и миссия проекта
├── plan.md              # Глобальный план развития
└── README.md            # Краткое описание проекта
```

## 🤖 Работа с AI-агентами

### Основной процесс работы

Когда вы хотите добавить новую функцию или исправить что-то:

1. **Планирование** → AI создаёт `artifacts/implementation_plan.md`
2. **Разбивка на задачи** → Заполняется `artifacts/task.md`
3. **Реализация** → AI пишет код, отмечая выполненные задачи
4. **Документация** → AI создаёт `artifacts/walkthrough.md` с отчётом

### 🔥 Специальные команды (Workflows)

#### `/spec` - Детальная проработка идеи

**Что делает:** Превращает вашу расплывчатую идею в детальную спецификацию через интервью.

**Как использовать:**

1. Создайте файл с вашей идеей:
```powershell
# Создайте папку для спецификаций
mkdir specs

# Создайте файл с идеей (любой текст)
echo "# Хочу добавить уведомления" > specs/notifications.md
```

2. Запустите команду в чате с AI:
```
/spec @specs/notifications.md
```

3. **AI начнёт интервью**, задавая вопросы:
   - 🎯 Что именно должно делать?
   - 🎨 Как должно выглядеть?
   - 🔧 Какие технологии использовать?
   - ⚠️ Какие могут быть проблемы?

4. **Результат:** AI перезапишет файл, превратив его в детальную спецификацию!

**Пример:**

```markdown
# БЫЛО (ваш файл specs/notifications.md):
Хочу систему уведомлений с красивыми анимациями

# СТАЛО (после /spec):
# Система уведомлений

## Обзор
Toast-уведомления с поддержкой 6 визуальных стилей...

## UI/UX
- Позиция: правый верхний угол
- Анимация входа: slide + fade
- Автозакрытие через 5 секунд
...

[Ещё 50+ строк детальной спецификации]
```

#### `/brainstorm` - Режим мозгового штурма

**Что делает:** Заставляет AI придумать несколько вариантов решения задачи (от простого до инновационного), сравнить их и выбрать лучший.

**Как использовать:**
1. Напишите: `/brainstorm Хочу добавить оплату картами`
2. AI проанализирует задачу и выдаст 3 стратегии.
3. Выберите понравившуюся, и AI начнёт реализацию.

---

## 🔄 Фазы разработки (Spec-first подход)

Когда проект усложняется, лучше использ овать **системный подход к разработке** из 4 фаз:

```
Spec → Draft → Simplify → Verify
 📋      ⚡       🧹         🔬
```

Каждая фаза решает свою задачу. Не обязательно проходить все - но для больших фич это сэкономит время.

### 1️⃣ Фаза: Spec (Спецификация)

**Цель:** Детально описать **ЧТО** нужно сделать, **ЗАЧЕМ** и **КАК**.

**Команда:**
```
/spec Добавить систему уведомлений
```
Или используйте шаблон:
```
/spec @templates/spec-template.md
```

**Что п роисходит:**
- AI задаёт вопросыпо 4 раундам:
  1. Основная механика - что делает фича?
  2. UI/UX - как выглядит?
  3. Техническая реализация - как работает?
  4. Граничные случаи - что может пойти не так?

**Результат:** Файл `specs/[название].md` с детальным описанием.

**Когда использовать:**
- ✅ Новая большая фича
- ✅ Непонятно c чего начать
- ✅ Команда из >1 человека (нужна общая картина)
- ❌ Маленькое изменение (<10 строк кода)

---

### 2️⃣ Фаза: Draft (Быстрый прототип)

**Цель:** Создать **работающий код** максимально быстро. "Работает > Красиво".

**Команда:**
```
/draft @specs/notifications.md
```

**Что происходит:**
- AI читает спецификацию (или спрашивает если её нет)
- Создаёт минимальный рабочий код
- Добавляет `// TODO:` комментарии для улучшений
- НЕ пишет тесты (это в фазе Verify)
- НЕ оптимизирует (это в фазе Simplify)

**Результат:** Рабочий прототип + отчёт `artifacts/draft-report.md`.

**Когда использовать:**
- ✅ Нужен быстрый MVP
- ✅ Проверить идею "в бою"
- ✅ Показать заказчику/коллегам
- ❌ Если нужен сразу production-ready код

**Философия:** Лучше работающий "некрасивый" код, чем идеальный код в голове.

---

### 3️⃣ Фаза: Simplify (Улучшение кода)

**Цель:** Сделать код **понятным**, **чистым** и **легко изменяемым**.

**Команда:**
```
/simplify @src/components/Notifications.jsx
```

**Что происходит:**
1. AI анализирует код и находит проблемы:
   - Дублирование кода
   - Длинные функции (>30 строк)
   - Magic numbers (непонятные числа)
   - Плохие названия переменных
2. Показывает **ПЛАН улучшений** (ничего не трогает!)
3. После вашего "да" применяет изменения
4. Показывает "было/стало" сравнения

**Результат:** Чистый код + отчёт `artifacts/simplify-report.md`.

**Когда использовать:**
- ✅ После Draft фазы
- ✅ Код работает, но сложно читается
- ✅ Перед передачей другим разработчикам
- ❌ Если код и так простой

**Важно:** AI **всегда спрашивает разрешение** перед изменениями!

---

### 4️⃣ Фаза: Verify (Тестирование)

**Цель:** **Убедиться**, что код работает правильно во всех ситуациях.

**Команда:**
```
/verify @src/components/Notifications.jsx
```

**Что происходит:**
1. AI создаёт план тестирования:
   - Happy path (всё хорошо)
   - Edge cases (граничные случаи)
   - Error cases (ошибки)
2. Создаёт автоматические тесты
3. Запускает их
4. Если находит баги - предлагает исправить

**Результат:** Тесты + отчёт `artifacts/verify-report.md`.

**Когда использовать:**
- ✅ Перед релизом
- ✅ После Simplify фазы
- ✅ Критичный функционал (платежи, безопасность)
- ❌ Если фича экспериментальная и может быть удалена

**Для zero-coder:** Тесты - это "автопроверка" кода. Один раз написал, потом AI сам проверяет при изменениях.

---

### 🔄 Полный цикл: `/pipeline`

Хотите пройти все 4 фазы автоматически? Используйте:

```
/pipeline Система уведомлений
```

**Что происходит:**
1. Spec → AI задаёт вопросы, создаёт спецификацию
2. Ждёт вашего подтверждения
3. Draft → AI создаёт прототип
4. Ждёт вашего подтверждения  
5. Simplify → AI улучшает код
6. Ждёт вашего подтверждения
7. Verify → AI создаёт тесты
8. Готово! 🎉

**Время:** 20-30 минут (зависит от размера фичи).

**Результат:** Production-ready код с тестами и документацией.

---

### 💡 Когда что использовать?

| Задача | Рекомендация |
|--------|--------------|
| Маленькое изменение (<10 строк) | Просто попросите AI |
| Средняя фича (новый компонент) | `/spec` → `/draft` |
| Большая фича (новый модуль) | `/pipeline` |
| Исправление бага | Опишите проблему → `/verify` |
| Улучшение существующего кода | `/simplify` |
| Сравнение вариантов | `/compare` |
| Вернулись после перерыва | `/context` |

---

### 📊 Дополнительные workflows

#### `/compare` - Сравнение подходов

Когда выбираете между вариантами:

```
/compare REST API vs GraphQL
/compare Хранить в localStorage vs база данных
```

AI создаёт детальную таблицу сравнения и даёт рекомендацию для вашего случая.

#### `/context` - Текущее состояние

Когда забыли где находитесь:

```
/context
```

AI покажет:
- Какие фичи в работе
- В каких фазах  
- Какие проблемы требуют внимания
- Рекомендуемые следующие шаги

**Фильтры:**
```
/context specs      # Только спецификации
/context artifacts  # Только артефакты
/context src        # Только код
/context tests      # Только тесты
```

---

## 🎭 Персоны и Навыки (Super AI)

Вы можете переключать "характер" AI для разных задач.

### 🔄 Автоматическое переключение (Фазовые персоны)

При использовании workflows AI автоматически меняет стиль работы:

- **`/spec`** → `Spec Analyst` 📋 - задаёт много вопросов
- **`/draft`** → `Rapid Prototyper` ⚡ - делает код быстро
- **`/simplify`** → `Code Simplifier` 🧹 - улучшает читаемость
- **`/verify`** → `Test Engineer` 🔬 - создаёт тесты

Вы увидите сообщение:
```
🔄 Переключаюсь в режим Spec Analyst
📋 Фокус: детальная спецификация
Буду задавать вопросы!
```

### Персоны (Роли)
Скажите AI: "Включи режим [Роль]", чтобы изменить его поведение:

- **🕵️ Debugger** - Дотошный поиск ошибок. Требует логи, не верит на слово.
- **🏗️ Architect** - Планирует на века. Думает о масштабируемости.
- **👨‍🏫 Mentor** - Объясняет каждое действие простым языком. Идеально для обучения.
- **🎨 UI/UX Designer** - Фокусируется на красоте и удобстве интерфейса.
- **🛡️ Security Expert** - Ищет уязвимости и защищает данные.

### Навыки (Skills)
Просите добавить специфические требования к работе:

**Базовые навыки (из rules.md):**
- **Concise** - "Отвечай кратко".
- **Simple** - "Объясняй как для 5-летнего".
- **Test-Driven** - "Сначала пиши тесты".
- **Security-First** - "Проверяй безопасность".

**Расширенные навыки (из `.antigravity/skills/`):**
- **context-engineering** - Оптимизация работы с контекстом AI
- **task-hierarchy** - Иерархия задач (Epic → Task → Sub-task)

> 💡 Скажите: "Применяй context-engineering" для активации навыка.

### Commit Convention (из Beads)

При коммите добавляйте ID задачи в скобках:

```bash
git commit -m "feat(auth): add login form (T1.2)"
git commit -m "fix(chart): tooltip position (T3)"
```

Это связывает коммит с задачей в `task.md`.

## 📝 Важные файлы для редактирования

### Для вас (редактируйте сами):

- **`mission.md`** - Опишите, зачем этот проект, какую проблему решает
- **`plan.md`** - Глобальный план развития проекта
- **`README.md`** - Краткое описание (то, что видят другие)
- **`changelog/changelog.md`** - История изменений (обновляет AI, но можно и вручную)

### Для AI (обычно не трогайте):

- **`AGENTS.md`** - Инструкции для AI-агентов
- **`ARCHITECTURE.md`** - Техническая архитектура
- **`.agent/`** - Вся папка с настройками AI

### Рабочие файлы (создаются автоматически):

- **`artifacts/`** - Временные файлы текущей сессии работы с AI
  - Можно удалять после завершения работы
  - AI пересоздаст их при необходимости

## 💡 Примеры типичных сценариев

### Сценарий 1: Добавить новую функцию

1. Опишите идею AI простыми словами:
   ```
   Хочу добавить форму регистрации пользователей
   ```

2. AI создаст план в `artifacts/implementation_plan.md`

3. Просмотрите план, подтвердите или попросите изменить

4. AI реализует код, обновляя `artifacts/task.md`

5. В конце AI создаст отчёт в `artifacts/walkthrough.md`

### Сценарий 2: Детально проработать идею с /spec

1. Создайте файл:
   ```powershell
   echo "# Добавить темную тему" > specs/dark-theme.md
   ```

2. Запустите интервью:
   ```
   /spec @specs/dark-theme.md
   ```

3. Отвечайте на вопросы AI

4. Получите детальную спецификацию

5. Используйте спецификацию для реализации:
   ```
   Реализуй то, что описано в specs/dark-theme.md
   ```

### Сценарий 3: Исправить баг

Просто опишите проблему:
```
В форме логина не работает кнопка "Войти" - ничего не происходит при клике
```

AI найдёт проблему и исправит.

## ⚙️ Настройка под свой проект

### При создании нового проекта из шаблона:

1. **Скопируйте шаблон** в новую папку

2. **Обновите основные файлы:**
   - `README.md` - название и описание проекта
   - `mission.md` - цель вашего проекта
   - `plan.md` - что планируете сделать
   - `package.json` / `requirements.txt` - зависимости

3. **Запустите настройку:**
   ```powershell
   .\scripts\setup.ps1
   ```

4. **Начинайте работу с AI!**

## 🎓 Советы по эффективной работе

### ✅ Делайте:

- **Будьте конкретны:** "Добавь кнопку удаления в каждую карточку товара" лучше, чем "улучши интерфейс"
- **Используйте /spec для сложных фич:** Это сэкономит время на переделки
- **Проверяйте планы:** Прочитайте `implementation_plan.md` перед реализацией
- **Сохраняйте контекст:** Работайте над одной фичей до конца

### ❌ Не делайте:

- **Не давайте расплывчатые задачи:** AI будет гадать и может ошибиться
- **Не редактируйте `AGENTS.md` без нужды:** Это сломает работу AI
- **Не удаляйте `.agent/`:** Там важные настройки
- **Не смешивайте задачи:** Закончите одно, потом начинайте другое

## 🆘 Частые вопросы

**Q: Что делать с папкой `artifacts/` после завершения работы?**  
A: Можете удалить - это временные файлы. AI создаст новые при следующей работе.

**Q: Можно ли редактировать `AGENTS.md`?**  
A: Лучше не надо, если не уверены. Это инструкции для AI, неправильные изменения могут сломать работу.

**Q: Как добавить свой workflow?**  
A: Создайте `.md` файл в `.agent/workflows/` по примеру `spec.md`. Формат:
```markdown
---
description: Краткое описание
---

# Название workflow

Инструкции что делать...
```

**Q: AI создаёт не то, что я хочу. Что делать?**  
A: Используйте `/spec` - это систематизирует вашу идею. Или опишите детальнее, добавьте примеры.

**Q: Можно ли использовать этот шаблон для любого языка программирования?**  
A: Да! Шаблон универсальный. Просто укажите AI какой язык использовать.

**Q: Что такое MCP серверы?**  
A: Model Context Protocol - расширения, которые дают AI новые возможности (умный поиск, проверка версий и т.д.). См. [docs/mcp-setup.md](docs/mcp-setup.md)

## 🔌 MCP серверы (расширенные возможности)

MCP (Model Context Protocol) - это способ дать AI "суперспособности" для работы с кодом.

### Рекомендуется: Smart Coding MCP

**Что даёт:**
- 🔍 **Умный поиск по коду** - AI понимает смысл, не только ключевые слова
- 📦 **Актуальные версии пакетов** - проверка npm, PyPI, Maven и 15+ других
- 🎯 **Лучшее понимание проекта** - AI эффективнее находит нужный код

**Установка:**
```powershell
npm install -g smart-coding-mcp
```

**Подробная инструкция:** [docs/mcp-setup.md](docs/mcp-setup.md)

💡 **Для zero-coder:** После установки вам ничего не нужно делать - AI автоматически начнёт использовать умный поиск!

## 🔗 Полезные ссылки

- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта
- [AGENTS.md](AGENTS.md) - Инструкции для AI (для любопытных)
- [.agent/workflows/](.agent/workflows/) - Все доступные workflows
- [templates/spec-template.md](templates/spec-template.md) - Шаблон спецификации
- [.antigravity/personas.md](.antigravity/personas.md) - Все персоны AI
- [.antigravity/skills/](.antigravity/skills/) - Дополнительные навыки

## 📚 Быстрая шпаргалка по командам

```bash
# Workflows (главные)
/spec @file.md          # Детальная спецификация через интервью
/draft @specs/file.md   # Быстрый прототип из спецификации
/simplify @src/file.js  # Улучшение существующего кода
/verify @src/file.js    # Тестирование кода
/pipeline [описание]    # Полный цикл всех 4 фаз

# Workflows (дополнительные)
/brainstorm [задача]    # Мозговой штурм, 3 варианта решения
/compare A vs B         # Сравнение подходов/технологий
/context                # Показать текущее состояние проекта

# Персоны (примеры)
Включи режим Spec Analyst
Работай как Debugger
Используй UI/UX Designer
```

---

**Помните:** Это ваш шаблон! Адаптируйте его под свои нужd. Главное - сохраняйте структуру `.agent/` и `.antigravity/` для корректной работы AI. 🚀
